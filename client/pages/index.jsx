import { Box } from '@mui/material';
import React from 'react';
import Layout from '../src/components/global/Layout';

const Home = () => {
  return (
    <Layout>
      Homepage
    </Layout>
  );
};

export default Home;